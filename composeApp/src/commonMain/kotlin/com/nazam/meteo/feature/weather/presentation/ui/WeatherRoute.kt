package com.nazam.meteo.feature.weather.presentation.ui

import androidx.compose.runtime.Composable

/**
 * Route = point de branchement pour la feature Weather.
 * Plus tard, ici on mettra le ViewModel + les actions.
 */
@Composable
fun WeatherRoute() {
    WeatherScreen()
}